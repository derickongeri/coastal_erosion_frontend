import { JsonItem } from "./JsonItem";
/**
 * Converts from JavaScript data (objects, strings, etc) to FracturedJson's DOM, to allow it to be formatted.
 */
export declare function ConvertDataToDom(element: any, propName?: string, recursionLimit?: number): JsonItem | undefined;
